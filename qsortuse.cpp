#include <stdlib.h>
#include <stdio.h>
#include <time.h>

int main(void){
	
/*	T tab[n];
	qsort(tab , n , sizeof (T) , COMP)  T - typ danej
 	COMP - callback - funkcja porównująca
	   int COMP(const void *a , const void *b)
		{
			const T *p = (T*)a , *q = (T*)b;
			return *p - *q;
		}
*/

	return 0;	
}
